name: devbox
services:

  devbox:
    image: devbox:latest
    restart: always
    networks:
      - devbox
    volumes:
      - devbox:/home/workspace

volumes:
  devbox:
    external: true

networks:
  devbox:
    external: true