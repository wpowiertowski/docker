{
  "configurations": [
    {
      "name": "Containers: Python - Hello",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "docker-run: debug",
      "python": {
        "pathMappings": [
          {
            "localRoot": "${workspaceFolder}/",
            "remoteRoot": "/app"
          }
        ],
        "projectType": ""
      }
    }
  ]
}