# Watchtower Email Notification Settings
# Copy this file to .env and fill in your actual values
# 
# SECURITY WARNING: The .env file contains sensitive credentials.
# - Do NOT commit the .env file to version control
# - Ensure the .env file is listed in .gitignore
# - Keep the file permissions restricted (chmod 600 .env)

# Email address to send notifications from
WATCHTOWER_EMAIL_FROM=watchtower@example.com

# Email address to send notifications to
WATCHTOWER_EMAIL_TO=admin@example.com

# SMTP server hostname
WATCHTOWER_EMAIL_SERVER=smtp.example.com

# SMTP server port (typically 587 for TLS or 465 for SSL)
WATCHTOWER_EMAIL_SERVER_PORT=587

# SMTP server username
WATCHTOWER_EMAIL_SERVER_USER=your-smtp-username

# SMTP server password
WATCHTOWER_EMAIL_SERVER_PASSWORD=your-smtp-password
