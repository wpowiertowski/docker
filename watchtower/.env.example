# Watchtower Pushover Notification Settings
# Copy this file to .env and fill in your actual values
# 
# SECURITY WARNING: The .env file contains configuration.
# - Do NOT commit the .env file to version control
# - Ensure the .env file is listed in .gitignore
# - Keep the file permissions restricted (chmod 600 .env)
# - Sensitive credentials (user key and app token) should be stored as Docker secrets
#   in the secrets/ directory - see compose.yml for instructions

# Pushover API URL (optional - defaults to Pushover's standard API)
# Leave as default unless using a custom Pushover server
WATCHTOWER_NOTIFICATION_URL=https://api.pushover.net/1/messages
